# .ai/
Agent-only workspace for logs, temporary artifacts, and decision notes.

Rules:
- Never commit secrets.
- If you need to store temporary outputs, use `.ai/artifacts/`.
- Put architecture decision records in `.ai/decisions/` (short markdown files).
